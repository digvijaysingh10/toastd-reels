import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Toastd Reels",
  description: "Toastd-Reels is a dynamic video reel platform designed to deliver an immersive, Instagram-like scrolling experience, where users can discover and engage with a wide variety of content. Whether you're watching the latest trends or discovering new products, Toastd-Reels combines seamless video playback with social interaction features, including likes, comments, and sharing.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
